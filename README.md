## Token Swap – Next.js + TypeScript + Tailwind (shadcn/ui)

### Getting started

- Run `nvm use` (repo has `.nvmrc` set to 22).
- Install deps: `npm install`.
- Dev server: `npm run dev` then open `http://localhost:3000`.

### Environment

- Set `NEXT_PUBLIC_FUNKIT_API_KEY=Z9SZaOwpmE40KX61mUKWm5hrpGh7WHVkaTvQJpQk` for local and Vercel.

### Tech

- Next.js (App Router), React 19, TypeScript strict
- Tailwind CSS v3, shadcn/ui (Radix UI)
- `@funkit/api-base` for token and price data
